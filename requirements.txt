# PDFScribe CLI dependencies

# Core dependencies
pdf2image>=1.16.0
python-dotenv>=1.0.0
requests>=2.28.0
tqdm>=4.64.0

# Google Drive integration
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0
google-auth>=2.15.0

# RAG support (optional - install with: pip install pdfscribe[rag])
sqlite-vec>=0.1.1
psycopg2-binary>=2.9.9
# openai>=1.0.0  # Only needed for embeddings

# REST API (Cloud Run)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
